#### centos 添加环境变量 

1 直接执行 export PATH=$PATH:/var/www/bin
当前回话生效

2 vim ~/.bash_profile 
PATH=$PATH:/var/www/bin

source .bash_profile

仅对当前用户生效

3 vim /etc/profile
PATH=$PATH:/var/www/bin
export PATH
source /etc/profile

对系统所有用户生效

4 查看全局环境变量
