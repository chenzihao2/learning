获取get值

url.parse(req.url,true).query


获取method
req.method

获取post值
req.on('data', function(chunk) {
        postData += chunk;
        })
req.on('end', function(e,data) {
        console.log(postData);
        })
